generator client {
  provider = "prisma-client-js"
}

datasource db {
  url      = env("DATABASE_URL")
}

model User {
  id           Int     @id @default(autoincrement())
  userID       String  @unique
  registeredAt String
  refreshToken String?
}
